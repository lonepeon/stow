[package]
name = "stow"
version = "0.1.0"
edition = "2021"
description = "un/symlink pakages from the STOW_DIR to the target directory"

[dependencies]
clap = { version = "4.3.0", features = ["derive", "env"] }
uuid = { version = "1.3.3", features = ["v4"] }
walkdir = "2.3.3"
